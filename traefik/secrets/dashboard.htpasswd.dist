# Example only. Generate a real dashboard.htpasswd before starting Traefik.
# Format: username:bcrypt-hash
# Example generation commands:
# docker run --rm httpd:2.4-alpine htpasswd -nbB admin 'change-me'
# htpasswd -nbB admin 'change-me'
admin:$2y$05$replace.this.with.htpasswd.output.or.generate.a.real.file
