# Beszel Hub
# https://beszel.dev/guide/hub-installation
# Hub variables reference: https://beszel.dev/guide/environment-variables#hub

# Docker image tag
IMAGE_TAG=latest

# Network
#HTTP_BIND_ADDR=127.0.0.1
HTTP_BIND_PORT=8391

# Data
#HOST_DATA_DIR=./data

# Public URL of the hub used in links, notifications, and reverse-proxy setups.
APP_URL=https://beszel.domain.tld

# Auto-authenticate this email and bypass normal login flow.
#AUTO_LOGIN=

# Allow viewing container inspect/log details in the UI.
#CONTAINER_DETAILS=true

# Add custom Content-Security-Policy header value.
#CSP=

# Disable password login (use external auth only).
#DISABLE_PASSWORD_AUTH=false

# Enable OTP MFA (true or superusers).
#MFA_OTP=false

# Make all systems visible/editable to all users.
#SHARE_ALL_SYSTEMS=false

# Trust this forwarded header as authenticated user email.
#TRUSTED_AUTH_HEADER=

# Auto-create users for OAuth2/OIDC logins.
#USER_CREATION=false

# Bootstrap first user email on initial startup.
#USER_EMAIL=

# Bootstrap first user password on initial startup.
#USER_PASSWORD=

# Send periodic health pings to an external heartbeat endpoint.
# Docs: https://beszel.dev/guide/heartbeat
#HEARTBEAT_URL=

# Interval between heartbeat pings in seconds.
#HEARTBEAT_INTERVAL=60

# HTTP method used for heartbeat pings (GET, POST, HEAD).
#HEARTBEAT_METHOD=POST

# Logging
#LOG_MAX_SIZE=5m
#LOG_MAX_FILE=5
