# Woodpecker CI

[Woodpecker](https://woodpecker-ci.org) is a lightweight CI server.

## Prerequisites

This stack requires the external Docker network `forge`. Create it before starting:

```shell
docker network create forge
```

See `../_docs/forge_network.md` for details. For general setup steps, use the root `README.md`.
