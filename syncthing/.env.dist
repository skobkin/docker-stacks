# see https://hub.docker.com/r/linuxserver/syncthing
PUID=0
PGID=0
TZ=Europe/Moscow

HOST_DATA_DIR=./data

# Web interface
HTTP_BIND_ADDR=127.0.0.1
HTTP_BIND_PORT=8384

# Compose helper mode:
#   default - standalone localhost-bound container
#   traefik - adds Traefik labels and joins the external Traefik network
#COMPOSE_VARIANT=default

# Optional Traefik settings used by COMPOSE_VARIANT=traefik
#TRAEFIK_HOST=syncthing.example.com
#TRAEFIK_ENTRYPOINT=websecure
#TRAEFIK_SERVICE_PORT=8384

LOG_MAX_SIZE=5m
LOG_MAX_FILE=5