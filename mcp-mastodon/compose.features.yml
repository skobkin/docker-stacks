services:
  default: {}

  proxy:
    networks:
      - proxy

  traefik:
    networks:
      - default
      - traefik
    labels:
      traefik.enable: "true"
      traefik.docker.network: "${TRAEFIK_NETWORK:-traefik}"
      traefik.http.routers.mcp-mastodon.rule: "Host(`${TRAEFIK_HOST}`)"
      traefik.http.routers.mcp-mastodon.entrypoints: "${TRAEFIK_ENTRYPOINT:-websecure}"
      traefik.http.routers.mcp-mastodon.middlewares: "${TRAEFIK_ACCESS_POLICY:-default-access@file}"
      traefik.http.services.mcp-mastodon.loadbalancer.server.port: "8080"

  traefik_proxy:
    networks:
      - default
      - proxy
      - traefik
    labels:
      traefik.enable: "true"
      traefik.docker.network: "${TRAEFIK_NETWORK:-traefik}"
      traefik.http.routers.mcp-mastodon.rule: "Host(`${TRAEFIK_HOST}`)"
      traefik.http.routers.mcp-mastodon.entrypoints: "${TRAEFIK_ENTRYPOINT:-websecure}"
      traefik.http.routers.mcp-mastodon.middlewares: "${TRAEFIK_ACCESS_POLICY:-default-access@file}"
      traefik.http.services.mcp-mastodon.loadbalancer.server.port: "8080"
