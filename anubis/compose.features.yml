services:
  default: {}

  traefik:
    networks:
      - default
      - traefik
    labels:
      traefik.enable: "true"
      traefik.docker.network: "${TRAEFIK_NETWORK:-traefik}"
      traefik.http.routers.anubis.rule: "Host(`${TRAEFIK_HOST}`)"
      traefik.http.routers.anubis.entrypoints: "${TRAEFIK_ENTRYPOINTS:-websecure}"
      traefik.http.routers.anubis.service: anubis
      traefik.http.services.anubis.loadbalancer.server.port: "${TRAEFIK_SERVICE_PORT:-8923}"

networks:
  traefik:
    external: true
    name: "${TRAEFIK_NETWORK:-traefik}"
