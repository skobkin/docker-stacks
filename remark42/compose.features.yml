services:
  default: {}

  traefik:
    networks:
      - default
      - traefik
    labels:
      traefik.enable: 'true'
      traefik.docker.network: '${TRAEFIK_NETWORK:-traefik}'
      traefik.http.routers.remark42.rule: 'Host(`${TRAEFIK_HOST}`)'
      traefik.http.routers.remark42.entrypoints: '${TRAEFIK_ENTRYPOINT:-websecure}'
      traefik.http.routers.remark42.middlewares: "${TRAEFIK_ACCESS_POLICY:-default-access@file}"
      traefik.http.services.remark42.loadbalancer.server.port: '${TRAEFIK_SERVICE_PORT:-8080}'
