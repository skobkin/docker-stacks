# Forgejo

[Forgejo](https://forgejo.org) is a lightweight self‑hosted Git forge.

## Prerequisites

This stack requires the external Docker network `forge`. Create it first:

```shell
docker network create forge
```

See the detailed guide in `../_docs/forge_network.md`. For general setup, follow the root `README.md`.
